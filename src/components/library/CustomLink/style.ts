import styled from "styled-components";

export const Link = styled.a`
  color: inherit;
  height: fit-content;

  text-decoration: none;
  width: fit-content;

  :visited,
  :hover,
  :active {
    color: inherit;
  }


  > * {
    color: inherit;
  }
`;
